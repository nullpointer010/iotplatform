Read `AGENTS.md` and `agent-workflow/README.md` before doing anything non-trivial.

The active ticket is the one in `agent-workflow/tickets/` whose `status.md` says `in-progress`.
Only that ticket's scope may be implemented. Surface anything else as a follow-up ticket.
