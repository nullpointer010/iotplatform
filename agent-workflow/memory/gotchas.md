# Gotchas

Traps and surprises encountered. Append one bullet per gotcha with a link to
the ticket where it bit us.

<!-- Example entry (delete once real entries exist):
- CrateDB does not support `ON CONFLICT`; use `INSERT ... ON DUPLICATE KEY UPDATE` equivalent or upsert via PK. (ticket 0002)
-->
