# Frozen reference

This folder is the original platform draft from project bootstrap. It is
**frozen** and kept only for historical context.

The live platform stack lives at the repo root under [`/platform/`](../../platform/)
and is driven by the root [`Makefile`](../../Makefile).

Do not modify or run anything in this directory.
