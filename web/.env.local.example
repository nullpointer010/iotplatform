# API base URL for the browser. Empty means same-origin: the dev server
# (http://localhost:3000) is reached through oauth2-proxy at http://localhost,
# which also proxies /api/* to the FastAPI container. Leave empty unless you
# are bypassing the auth proxy on purpose.
NEXT_PUBLIC_API_BASE_URL=
